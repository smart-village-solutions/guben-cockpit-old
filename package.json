{
  "name": "guben-cockpit",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Guben Cockpit",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "typecheck": "cd frontend && npm run typecheck",
    "test": "npm --prefix content-gateway test && npm --prefix frontend test",
    "test:coverage": "npm --prefix content-gateway run test:coverage && npm --prefix frontend run test:coverage"
  }
}
